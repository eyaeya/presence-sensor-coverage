# 开发进度与检查记录

## Task 1 — 骨架+布局+Geo向量库+测试框架
- index.html 三区布局、Geo 向量库、Tests(?test) 框架完成
- 测试：TESTS: 3/3 passed（Playwright 读 #test-output / console 验证）
- 视觉：三区布局正常，无 console error
- SHA: n/a (git unavailable in sandbox)

## Task 2 — State 模块
- 在 Geo 后、Tests 前插入 State IIFE（defaults/applyMount/MOUNT_DEFAULTS）
- 新增 Tests.extra（Task2 起累积断言）；创建 tools/run-tests.mjs 持久化测试运行器
- 测试：TESTS: 8/8 passed（node tools/run-tests.mjs，exit 0）
- SHA: n/a (git unavailable)

## Task 3 — Geo.beamFrame
- Geo 内新增 beamFrame（三种安装的 S/d/u/v），加入 Geo 返回对象
- Tests.extra 追加 Task3 断言（在 Task2 块之后）
- 测试：TESTS: 16/16 passed（node tools/run-tests.mjs，exit 0）
- SHA: n/a (git unavailable)

## Task 4 — Geo.footprint
- Geo 内新增 footprint（椭圆锥沿β采样∩平面z=h，可选轴向截断，朝上射线退远界），加入 Geo 返回对象
- Tests.extra 追加 Task4 断言（Task3 块之后）
- 测试：TESTS: 20/20 passed（node tools/run-tests.mjs，exit 0）
- SHA: n/a (git unavailable)

## Task 5 — Geo.clipToRoom
- Geo 内新增 clipToRoom（Sutherland–Hodgman 裁剪到房间矩形），加入 Geo 返回对象
- Tests.extra 追加 Task5 断言（Task4 块之后）
- 测试：TESTS: 23/23 passed（node tools/run-tests.mjs，exit 0）
- SHA: n/a (git unavailable)

## Task 6 — Render 坐标变换+房间+引导
- State 后插入 Render IIFE（makeTransform/init/draw/currentTransform）；正常引导接入（建 state、init、draw、resize 重绘）
- Tests.extra 追加 Task6 transform 断言（Task5 块之后）
- 测试：TESTS: 25/25 passed（node tools/run-tests.mjs，exit 0）
- 视觉：base64 data: URL 截图确认居中房间矩形+尺寸标注，无 console error
- SHA: n/a (git unavailable)

## Task 7 — Render 洋葱4层+clipPath
- Render 内新增 LAYERS/layerPolys/polyPoints/ensureClip/drawLayers；draw 末尾调 drawLayers；Render 返回加 layerPolys
- Tests.extra 追加 Task7 断言（Task6 块之后）
- 测试：TESTS: 28/28 passed（node tools/run-tests.mjs，exit 0）
- 视觉：base64 data: URL 截图确认4层嵌套半透明、未溢出房间，无 console error
- SHA: n/a (git unavailable)

## Task 8 — Render 存在/运动虚线
- Render 内新增 boundaryPoly/drawBoundaries（蓝=存在@750+rangePresence，橙=运动@0+rangeMotion，裁房间）；draw 内 drawLayers 后调 drawBoundaries；Render 返回加 boundaryPoly
- Tests.extra 追加 Task8 断言（Task7 块之后）
- 测试：TESTS: 31/31 passed（node tools/run-tests.mjs，exit 0）
- 视觉：base64 data: URL 截图确认橙(运动)外/蓝(存在)内虚线，未溢出房间，无 console error
- SHA: n/a (git unavailable)

## Task 9 — 传感器手柄+信息栏
- Render 新增 drawSensor（白点+朝向线），draw 内 drawBoundaries 后调；新增 Info IIFE（positioning/params/hover/render）；引导 Render.draw 后调 Info.render，resize 同步刷新
- Tests.extra 追加 Task9 断言（Task8 块之后）
- 测试：TESTS: 35/35 passed（node tools/run-tests.mjs，exit 0）— 注：计划文本写 36 系按 Task8 基线 32 推算，但 Task8 实际累计 31（见 Task8 条目），Task9 逐字加 4 断言 → 35；按真实结果记录，未改 Info/未改 runner
- 视觉：base64 data: URL 截图确认中心白点+朝向线、右栏三节信息，无 console error
- SHA: n/a (git unavailable)

## Task 10 — Interact 工具栏控件
- Info 后插入 Interact IIFE（clamp/num/seg/roomCtl/hAngleCtl/hRange/rebuild/init）；引导接入 Interact.init（onChange→重绘+刷新信息）
- Tests.extra 追加 Task10 clamp 断言（Task9 块之后）
- 测试：TESTS: 39/39 passed（node tools/run-tests.mjs，exit 0；基线35+4）
- 视觉：base64 data: URL 截图确认工具栏控件齐全、FOV变窄、切侧装/墙角控件按规则启停，无 console error
- SHA: n/a (git unavailable)

## Task 11 — 传感器放置+hover
- Interact 新增 nearestWall/nearestCorner/placeSensor/relocateSideWall（加入返回对象）；引导内绑定 svg 指针（吸顶拖拽/侧装沿墙/墙角点击/hover 读数→Info.render）
- Tests.extra 追加 Task11 放置断言（Task10 块之后）
- 测试：TESTS: 45/45 passed（node tools/run-tests.mjs，exit 0；基线39+6）
- 视觉：page.setContent 交互确认吸顶拖拽/侧装沿墙/墙角指向室内/hover读数，未溢出房间，无 console error
- SHA: n/a (git unavailable)

## Task 12 — 终验+收尾
- 缺陷修复：roomCtl 非吸顶分支改房间后 rebuild()（消除 height/hAngle 控件陈旧显示）
- 防御注释：drawBoundaries 标注依赖 drawLayers 先建 #roomClip
- 加固：Tests.extra 追加 15 条断言（frame正交性/φ90、洋葱面积嵌套+全在房间、运动>存在严格、Info数值/params/corner标签、clamp默认与空串、nearestWall右/下、relocateSideWall）
- 测试：TESTS: 60/60 passed（node tools/run-tests.mjs，exit 0；基线45+15）
- 巡检：黄金路径(吸顶/侧装/墙角)+9项边界(极值FOV/距离、墙角省层、极小房间、ψ±90)逐项通过，全程 0 越界点、0 console error
- 交付：单一 index.html，双击即用，无需服务器/构建/依赖
- SHA: n/a (git unavailable)

## Final review fix — I-1 + minors
- I-1（Important，跨模块）：roomCtl 改房间尺寸时 applyMount 误重置 height/hAngle/wall/corner（spec §7.1 仅切换安装方式才重置）。修复：W/D handler 改用 placeSensor(st,{x,y}) 只按现有 wall/corner 重新吸附+钳制传感器，不动其它状态
- M-1：移除 hRange 中死变量 var m=State.MOUNT_DEFAULTS
- M-4：bindPointer 增加 pointercancel→dragging=false 防护
- 测试：TESTS: 60/60 passed（node tools/run-tests.mjs，exit 0，未回归）
- 校正：上方 Task 12 条目所述 roomCtl 修复仅解决控件陈旧显示，真正的状态丢失由本次 I-1 修复
- SHA: n/a (git unavailable)

## Final review fix #2 — I-2 (corner flip on room resize)
- I-2（Important，残留）：I-1 修复用 placeSensor，其 corner 分支经 nearestCorner 在房间某维>2倍增大时会翻转 st.corner（spec §7.1 仅切换安装方式才重置）。修复：新增 Interact.resnapRoom——side 仍用 placeSensor（按 st.wall 吸附+钳制，正确），corner 直接按现有 st.corner 重钉传感器、绝不调用 nearestCorner；roomCtl 两个 handler 改调 resnapRoom
- 回归断言：新增 'resnap corner preserved on grow'（br 角、W 1000-默认4000→20000，断言 corner 仍 br、sensor (20000,0)）→ TESTS 60→61
- 已知非阻塞 minor 仍在：侧/角模式连续输入房间 W/D 时 rebuild() 会丢输入焦点（滑杆/微调可用，无数据丢失）；pointermove 全量重绘（性能）；部分断言依赖默认对称
- 测试：TESTS: 61/61 passed（node tools/run-tests.mjs，exit 0，无回归）
- SHA: n/a (git unavailable)

## Polish — 3 非阻塞 minor
- m1（焦点丢失）：I-1/I-2 修复后 resnapRoom 不再改 height/hAngle/wall/corner，roomCtl 的 rebuild() 已多余 → 移除；新增 W/Dn 的 change 事件把钳制后的房间值回写输入框（输入中不打断，commit 时纠正显示）。焦点不再丢失
- m2（性能）：pointermove 仅在拖拽确实移动传感器时才 Render.draw；纯 hover 只 Info.render（pointerdown 仍全量重绘）
- m3（断言强度）：新增 'info side end values asym'（左墙 y=500 → 距两端 500/2500 非对称）真正区分两端逻辑，TESTS 61→62
- 测试：TESTS: 62/62 passed（node tools/run-tests.mjs，exit 0，无回归）
- SHA: n/a (git unavailable)

## Code review fix — axial range clipping + hover bounds
- 图像计算根因：Geo.footprint 在带 axialMax 时把射线交点钳到轴向距离平面，但该点不再位于目标检测高度 z=h；吸顶 height=5000、presence@750、range=3000 时本应无蓝色存在边界，却会错误画出投影边界
- 修复：footprint 先求目标高度平面上的完整足迹，再用 r·d 的轴向距离半空间裁剪到 0 <= r·d <= R；新增 Geo.clipByAxialDistance
- 需求级小修：Info.hover 对房间外点返回 null，避免在 SVG 留白处显示负数/超界距墙读数
- 回归断言：新增 'boundary ceiling beyond axial range empty' 与 'info hover outside room null'，TESTS 62→64
- 视觉组合巡检：通过真实页面控件跑 33 个组合（吸顶/侧装/墙角，高度/FOV/倾角/水平角/极小房间），0 个 SVG 点越过房间矩形；height=5000 的 4 个吸顶组合蓝色存在边界均为 0
- 测试：TESTS: 64/64 passed（node tools/run-tests.mjs，exit 0）；UI 组合结果保存到 artifacts/ui-combo-results-final.json
- SHA: n/a (not a git repository)

## User-reported fix — 3D range projection + arc-only dashed boundaries
- 用户复现参数：侧装，房间 7000 x 5000，传感器在底墙 x=5829.137mm，安装高度 1500/1800mm，下倾 20°，水平角 0°，H/V FOV 160°/60°，存在/运动距离 3000/5000；旧虚线出现折叠，且存在边线顶视距离错误按约 3000mm 处理
- 根因校正：存在/运动距离应按传感器到目标点的真实 3D 距离解释；目标高度平面上的顶视半径应为 sqrt(R^2 - (sensorHeight - detectedHeight)^2)，不是轴向投影距离
- 修复：Geo.footprint 的带距离分支改为与目标高度平面上的圆盘相交（clipByRangeDistance / clipToCircle / clipToConvex），不再使用轴向距离平面裁剪
- 视觉修复：drawBoundaries 不再绘制检测区域 polygon 的完整外轮廓，新增 Render.boundaryCurveSegments，仅绘制 3D 距离 = R 的圆弧段，避免把 FOV 边/房间裁剪边误画成距离边线
- 回归断言：新增 side presence/motion 3D range h1800、side bottom presence 3D range h1500、side bottom presence range arc only，TESTS 64→68
- 审查补强：清理旧 axialMax 注释/测试名，新增 side left boundary arcs match 3d range h1800，TESTS 68→69
- 浏览器验证：对应底墙复现场景中 presenceMaxPlan=2904.737509655564，presenceLimit=2904.7375096555625；motionMaxPlan=4769.69600708473，motionLimit=4769.696007084728；dashedPolygons=0，dashedPolylines=2；截图 output/playwright/user-case-bottom-fixed-arc.png
- 组合验证：真实浏览器跑 12 组吸顶/侧装/墙角/FOV/小房间组合，failures=[]，所有 SVG 点均未越界，所有虚线段均满足 3D 距离投影约束
- 测试：TESTS: 69/69 passed（node tools/run-tests.mjs，exit 0）
- SHA: n/a (not a git repository)

## 2026-05-20 — Task 1 modular split
- 初始化 git，基线提交：9698f93 chore: establish project baseline
- 当前工作分支：refactor/modular-geometry-validation
- 结构调整：index.html 保留 HTML/CSS 和普通 script src；原内联脚本按职责拆为 src/geo.js、src/state.js、src/render.js、src/info.js、src/interact.js、src/tests.js、src/app.js
- 测试 runner：tools/run-tests.mjs 改为解析 index.html 中 script src 顺序并在 VM 中逐个加载，避免脚本顺序漂移
- 测试：TESTS: 69/69 passed（node tools/run-tests.mjs，exit 0）
- SHA: 2b7da17

## 2026-05-20 — Task 2 inverse geometry validation
- 新增 Geo.rangeProjectionRadius 与 Geo.inBeamAtHeight，统一 3D 距离投影和锥体点分类逻辑，Render.boundaryCurveSegments 改复用 Geo helper
- Tests.extra 新增 4 条 15000x10000 逆向数学断言：3D 距离投影半径、垂直差超距为空、侧装中心线三角函数、侧装大房间存在距离弧半径
- 新增 tools/geometry-inverse-cases.mjs：生成并验证 141 个用例，覆盖吸顶/侧装/墙角、四面墙/四角、高度/FOV/下倾/水平角/距离边界、用户底墙复现场景；输出 artifacts/geometry-inverse-results.json
- 验证内容：房间裁剪、正交基、真实 3D 距离、圆弧点在锥体内、高分辨率采样对照防漏窄弧、开放截面采样点分类假阳性检查
- 文档：设计文档从旧“轴向距离截断”更新为真实 3D 欧氏距离模型，并记录虚线只绘制真实距离弧线
- 测试：TESTS: 73/73 passed（node tools/run-tests.mjs，exit 0）；GEOMETRY_INVERSE: 141 cases, 0 failures（node tools/geometry-inverse-cases.mjs，exit 0）
- SHA: 147f591

## 2026-05-20 — Task 2 review hardening
- 按 spec/code review 修正：吸顶用例增加 359°、近墙位置和 3000/5000、5000/8000 距离组合；侧装/墙角增加两组距离边界组合；新增左/右墙、上/下墙、墙角 BL/BR、BL/TL 镜像等价断言
- Render.boundaryCurveSegments 从 360 采样改为 7200 采样，并在进入/退出可见弧段时二分求交点，减少窄弧漏绘与端点短缺
- tools/geometry-inverse-cases.mjs 的 oracle 改为脚本内独立 3D 锥体公式；增加反向覆盖采样，避免空 polygon 或漏覆盖误通过
- artifacts/geometry-inverse-results.json 改为本地生成报告，不再纳入 git，避免每次运行产生大体积 diff
- 测试：TESTS: 73/73 passed（node tools/run-tests.mjs，exit 0）；GEOMETRY_INVERSE: 321 cases, 0 failures（node tools/geometry-inverse-cases.mjs，exit 0）
- SHA: f904408

## 2026-05-20 — Task 4 browser visual verification
- 使用独立 headless Chrome 访问 http://127.0.0.1:4173/index.html，通过真实按钮/输入框/鼠标点击驱动页面，不依赖 VM 内部状态
- 验证 5 个场景：默认吸顶、15000x10000 吸顶 H5000、15000x10000 侧装、用户底墙复现场景、15000x10000 墙角
- 结果：所有 SVG polygon/polyline 点均在房间矩形内；侧装/墙角/用户复现场景的虚线 polyline 点 3D 距离误差最大约 2.73e-12 mm；failures=[]
- 用户复现场景：房间 7000x5000，底墙 sensor.x≈5829.1368，presence/motion 两条虚线均为真实 3D 距离弧线（3000/5000mm）
- 产物：artifacts/browser-geometry-results.json；output/playwright/geometry-default-ceiling.png、geometry-user-repro-bottom.png、geometry-corner-15000.png
- 注：浏览器控制台仅有 favicon 404，不影响工具脚本与 SVG 渲染
- 验证基线提交：f74abbb

## 2026-05-20 — finite color footprint and UI controls
- 彩色洋葱层改为 `FOV ∩ 运动距离 ∩ 房间`，不再按无限 FOV 足迹向远处铺满；新增回归覆盖 15000x10000 侧装大房间、运动距离截止、运动距离低于垂直高度差时空层。
- 数字输入框拆分“编辑中”和“提交数值”：退格清空不会被旧值立即回填，合法数字输入会实时同步状态与滑杆，失焦/change 时再钳制回写。
- 底部工具栏按空间/安装/视场/距离分组；右侧信息栏新增房间小示意图、参数 chips、鼠标四向距离格；图例改为常驻 6 项。
- 逆向验证升级：`tools/geometry-inverse-cases.mjs` 的彩色层真值改为 `inBeam && 3D distance <= rangeMotion`。
- 浏览器复验：15000x10000 侧装场景 4 个彩色层均满足各自高度的 3D 运动距离上限，图例/分组/右栏 chips 均渲染；截图 `output/playwright/finite-layer-side-15000.jpg`。

## 2026-05-20 — UI Review（右栏 240px / 罗盘 / 覆盖区 / 传感器预设）
- 范围扩展：vFov 上限 90°→160°；rangePresence 上限 5000mm→6000mm。`Interact.rebuild` 内两条 `num(...)` 同步更新 label 与 max。
- 新建 `src/presets.js`：3 型号 5 variant 挂在 `window.SensorPresets`；index.html 在 `state.js` 之前引入。
- `Interact.applyPreset(state, variant)`：先 `State.applyMount` 兜默认，再钳值覆盖 variant 指定的字段；安全可在 `Interact.init` 之前调用（测试场景）。`variantMatchesState` 决定预设按钮的 `.on` 状态；apply/match 严格对称（ceiling variants 显式写 tilt:0，避免隐式重置导致的不对称）。
- 工具栏新增第 5 组「传感器预设」，沿用原 `.tool-group { border-right }` 衬线分隔；每行 model-name + variant 小按钮。
- 右栏：宽 320→240；chips 由 2×2 改 1×4；删 hover-grid，改为 hover-compass（↑/←/→/↓ 四数 + 中央 mini-room 预览点）与 coverage（站/坐/躺/地 四色点）。
- `Info.coverage(st, mm)` 复用 `Geo.inBeamAtHeight` + 真实 3D 距离判定，遍历 `Render.LAYERS` 而非自带高度字面量，避免与渲染层漂移。
- 测试：85 → 101（+16，spec 估 +13，差异：Task 1 多 2 条 clamp 保护、Task 3 角点 stand/ground 实测后从 1 条占位拆 2 条真值）。TESTS: 101/101 passed。
- 浏览器复验：`superpowers-chrome` MCP 真实点击两个场景 — 小米人在 Pro·吸顶（mount/H/V/距离/高/倾 7 字段全对，#info=240px，5 组工具栏，预设按钮 `.on`，hover 中心罗盘 1500/2000/2000/1500，覆盖区 4 点全亮）；子擎 Trio·侧装（wall=left，sensor=(0,1500)，同上 hover/coverage）。沙箱阻断 TCP 监听与 Playwright file://，改走 chrome MCP 加载 file:// 直接读取。
- 产物：`artifacts/ui-review-presets.json`，`output/playwright/ui-review-pro-ceiling.png`、`output/playwright/ui-review-trio-side.png`。
- SHA: 56b5f39（包括前置 c0ae26a/6c6cd80 等共 9 个提交）

## 2026-05-20 — 传感器预设：下拉 UI + Lite/2S 新增
- 数据：`src/presets.js` 末尾新增 子擎 Lite（吸顶 / 侧装，130°/130° FOV，P 4000 / M 8000，tilt:0）与 小米人体 2S（侧装 130°/130° FOV、P 3000 / M 8000、高度 200mm、tilt:0）。下拉总条目数 5 → 8。
- 高度范围：side / corner 下限 1000 → 200mm（`hRange` + `applyPreset` 两处 lim 字面量同步），ceiling 仍 [2000, 5000]。
- UI：`Interact.presetsGroup()` 由 "5 个按钮行" 改为 "下拉 + 使用 + 当前指示"。下拉选项格式 `设备名 / 顶装|侧装|角装`，简称映射 `{ceiling:'顶装',side:'侧装',corner:'角装'}` 在 `presetsGroup` 内部。Interact IIFE 顶部加 `lastPresetKey` 让 rebuild 后下拉选择不丢；"使用" 按钮始终启用、点击时 `lastPresetKey = sel.value` 再 `applyPreset`。"当前: …" 行由 `variantMatchesState` 实时计算，不匹配显示 "—"。
- CSS：`index.html` 删 7 行 `.preset-row/.preset-name/.preset-variants/.preset-btn*`；新增 7 行 `.preset-row1/.preset-select/.preset-apply/.preset-apply:hover/.preset-current/.preset-current span`，`.preset-container` `min-width` 170→200。
- 测试：101 → 110（+9：1 clamp 保护 + 4 新 variant apply + 4 DOM/当前指示）。其中 `preset-current` span 查找用了 tag 而非 `children[1]` 索引，避免真实 DOM 与 mock 之间 `children` 含义不同的隐患。TESTS: 110/110 passed。
- 浏览器复验：`superpowers-chrome` MCP 在 1440×900 file:// 跑两个场景 — 子擎 Lite / 顶装（state 7 字段 + selectValue 保持 + 当前指示文本）；小米人体 2S / 侧装（height=200 + 安装高度 num min='200' + 当前指示文本）。
- 产物：`artifacts/ui-presets-dropdown.json`，`output/playwright/ui-presets-dropdown-lite-ceiling.png`、`output/playwright/ui-presets-dropdown-2s-side.png`。
- SHA: 458c739（含前置 97347cf/b441532/e8e92da/cdbd3c4/5451f72，共 6 个实现提交）
